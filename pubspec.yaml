name: auracast_assistant
description: "Example application for an auracast assistant."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.17.1
  cupertino_icons: ^1.0.6
  equatable: ^2.0.5
  flutter_reactive_ble: ^5.3.1
  permission_handler: ^10.4.3
  provider: ^6.1.2
  rxdart: ^0.27.7


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0



# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

